"""
量化引擎配置管理
集中管理所有因子计算相关的配置参数
"""
import logging

class CalculatorConfig:
    """因子计算器配置类"""

    # ================= 计算周期配置 =================
    # RPS计算周期（统一）
    RPS_PERIODS = [5, 10, 20, 50, 120, 250]

    # ================= 数据加载配置 =================
    # 增量更新窗口大小（天数）
    INCREMENTAL_WINDOW_DAYS = 400  # 计算250日RPS，往前推400天

    # 保存时保留的最新天数
    SAVE_RECENT_DAYS = 3

    # ================= 数据库配置 =================
    USE_TRANSACTION = True
    CHUNK_SIZE = 50  # 批量插入大小（SQLite限制：999/14≈71行，设置为50安全）

    # ================= 板块配置 =================
    # 板块黑名单（包含这些关键字的板块会被过滤）
    SECTOR_BLACKLIST = [
        "昨日", "连板", "涨停", "ST", "AB股",
        "昨日涨停", "昨日连板", "含一字", "炸板"
    ]

    # ================= 日志配置 =================
    LOG_LEVEL = logging.INFO

    # ================= 性能配置 =================
    # 数据加载批次大小
    LOAD_BATCH_SIZE = 10000

    # 是否显示进度条
    SHOW_PROGRESS = True
